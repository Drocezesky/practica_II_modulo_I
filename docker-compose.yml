services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: questions_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

# psql -h localhost -p 5432 -U admin -d questions_db 
# consultar tablas \dt
# consultar pregunta select * from questions where id = NUM;

volumes:
  postgres_data:
